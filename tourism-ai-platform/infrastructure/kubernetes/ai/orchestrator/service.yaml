apiVersion: v1
kind: Service
metadata:
  name: orchestrator-svc
  namespace: ai
  labels:
    app: orchestrator-svc
spec:
  selector:
    app: orchestrator-svc
  ports:
    - name: http
      port: 8080
      targetPort: 8080
      protocol: TCP
