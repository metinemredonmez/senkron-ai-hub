apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-vision
  namespace: ai
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: ai-vision.health-tourism.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ai-vision
                port:
                  number: 8400
