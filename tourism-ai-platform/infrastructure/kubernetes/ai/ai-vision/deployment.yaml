apiVersion: apps/v1
kind: Deployment
metadata:
  name: ai-vision
  namespace: ai
  labels:
    app: ai-vision
spec:
  replicas: 2
  selector:
    matchLabels:
      app: ai-vision
  template:
    metadata:
      labels:
        app: ai-vision
    spec:
      containers:
        - name: ai-vision
          image: ghcr.io/health-tourism/ai-vision:v2.0.0
          imagePullPolicy: IfNotPresent
          envFrom:
            - configMapRef:
                name: ai-vision-config
            - secretRef:
                name: ai-vision-secrets
          ports:
            - containerPort: 8400
          readinessProbe:
            httpGet:
              path: /health
              port: 8400
            initialDelaySeconds: 10
            periodSeconds: 5
          livenessProbe:
            httpGet:
              path: /health
              port: 8400
            initialDelaySeconds: 30
            periodSeconds: 10
          resources:
            requests:
              cpu: 150m
              memory: 256Mi
            limits:
              cpu: 400m
              memory: 512Mi
