apiVersion: v1
kind: Service
metadata:
  name: ai-vision
  namespace: ai
  labels:
    app: ai-vision
spec:
  selector:
    app: ai-vision
  ports:
    - name: http
      port: 8400
      targetPort: 8400
      protocol: TCP
