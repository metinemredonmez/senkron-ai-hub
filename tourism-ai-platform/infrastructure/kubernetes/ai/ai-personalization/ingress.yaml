apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-personalization
  namespace: ai
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: ai-personalization.health-tourism.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ai-personalization
                port:
                  number: 8500
