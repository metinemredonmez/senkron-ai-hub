apiVersion: v1
kind: Secret
metadata:
  name: ai-nlp-secrets
  namespace: ai
type: Opaque
stringData:
  OPENAI_API_KEY: "${OPENAI_API_KEY}"
  MINIO_ACCESS_KEY: "${MINIO_ACCESS_KEY}"
  MINIO_SECRET_KEY: "${MINIO_SECRET_KEY}"
