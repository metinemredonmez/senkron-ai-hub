apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: ai-nlp
  namespace: ai
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
    - host: ai-nlp.health-tourism.local
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: ai-nlp
                port:
                  number: 8200
